# Alina Zubair — Portfolio
Edit `src/data/portfolio.ts` to change content. Put your resume in `public/` as `Alina-Zubair-Resume.pdf`.
Run locally (optional): `npm install`, `npm run dev`. Build: `npm run build`.
Optional contact form: set `VITE_FORMSPREE_ID` (see `.env.example`).
